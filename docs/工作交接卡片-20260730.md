# WDP 团队工作台 · 工作交接卡片

> 用途：上下文过长退役本会话，开新会话时把本卡片贴入即可接续。生成时间：2026-07-30

## 一、项目定位
- 仓库根：`E:\wdp-team-hermes`（盘符 E）
- 三仓库同步：①工程根 GitHub morking48 ②deploy=gitlab Maguanjie/wdp-team-platform ③knowledge=gitlab Maguanjie/wdp-team-knowledge（push 需 token + `-c http.sslVerify=false`）
- **自研模块改动必三处同步**：`web-ui/<f>` → `deploy/wdp-team-platform/webui-overlay/<f>` + `deploy/wdp-team-platform/web-ui/<f>`；SOUL 母本 `hermes-home/SOUL.md` → `deploy/wdp-team-platform/team-config/SOUL.md.team`；团队 skill/脚本同步到 team-config/
- 交付物正文与回复一律中文

## 二、今天已完成并已提交的大批改动
今天全部改动已提交 push（工程根 dd8cc6a / deploy 760a1aa / knowledge 7268710）。核心：
1. **判类体系**：双维度→硬信号判据（有责任人/版本规划/验收交付/方案在落地→需求；纯线索没人接没排期→signals）。**决策类彻底移除**（判类/审核/归并/submit_review choices/前端下拉全清，只在 signals/requirements/designs/projects 四类选）。设计收口（沉淀入库不主动判 designs，只认设计模式产出或带文档链接）。沉淀标签「清洗信号」→「沉淀入库」。
2. **项目全链路**：agent 可直接开档（submit_review --category projects）；项目需求直路（related_project 命中已开档项目→审核入库自动进 projects/<项目>/requirements/ 即 PREQ）；项目信息逐步补充（update_type: project_info + 乐观锁 + 审核更新档案不新建）；**入库去向显式分池**（审核表单「入库去向」下拉：公共需求池/项目需求池，选项目池出项目下拉）；未开档→409 PROJECT_NOT_FOUND 告警+开档引导；**客户级去重防冗余开档**（同 customer 已有项目→409 CUSTOMER_HAS_PROJECT，前端确认才带 force 强开，统一走 wbCreateProjectSafe）；SOUL 规训 agent 提交项目需求前先 query_knowledge.py --projects 用准确项目名。
3. **其他**：成员画像收敛（写能力+负责领域，不写任务流水/具体功能名）；历史会话多步 agent 输出归组渲染（方向A：同一轮多段拼一个气泡）+ 侧栏计数口径改为气泡数（localStorage key wb_visible_cnt_v2）；审核对话防串联（后端 approve/reject 主动清对话 + 重提检测 + few-shot 隔离规训）；权限门控修复（项目编辑按钮 window.__wb.IS_ADMIN + show() 软拦截）；飞书文档读取脚本 hermes-home/scripts/feishu_doc.py（团队级凭据 integrations.json，gitignore 不进仓库）。

## 三、当前环境状态（干净基线）
- 业务数据全 0（信号/需求/设计/决策/项目/PREQ 均 0，inbox 0）
- **保留**：BIM 设计 session（2dd9a22e0b79，用户演示用）、账号、飞书凭据、18 人团队画像、5 个团队 skill
- 用户正在**重新走全流程测试**

## 四、待验证/遗留点（下一步）
1. **需求 badge 与列表口径矛盾（已知，未改）**：工作台统计**含项目 PREQ**（用户明确要求统计包含项目，PREQ 计入需求 badge），但需求 tab 列表只显示公共需求池、不显示 PREQ → 数字可能和列表对不上。用户暂接受空库测试；将来若要对齐，让需求列表也显示项目 PREQ（带项目标记）。**不要再把 PREQ 排除出统计**（那是改错过一次的方向）。
2. **对话版审核（wb5.js）入库去向未对齐**：基础版审核（wb2.js doApprove）已做「入库去向分池+未开档引导」，对话版审核只做了 related_project 透传，未加入库去向下拉，下轮可对齐。
3. 用户重测中，关注：判类是否需求为主/纯线索才信号/不再出决策/项目需求进 PREQ/未开档告警/客户去重。

## 五、关键工作纪律（用户偏好）
- 部署类用户坚持自己手动启停，AI 不后台持有进程；测试用 8798 临时实例，测完必 kill+清测试数据
- 改动基于目的、不为改而改；诚实评估价值，区分单次 vs 扩展性
- 每轮改动：语法检查（node --check / ast.parse 用 agent-src/.venv/Scripts/python.exe）+ pyright（cd web-ui && npx pyright，自研模块 0 error）+ E2E 实测（真起服务真调 API）+ 三处同步 + 清测试数据
- 重置环境要问清「项目是否也清」——用户"重置"通常指含项目全清
- 沉淀 session 可删，BIM 设计 session 必须保留
