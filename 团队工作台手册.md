# WDP 产品团队 AI 工作台 — 核心手册

> 本文件是 WDP 产品团队 Hermes 工作台的纲领性文档。
> 团队成员通过本工作台完成信号收集、需求管理、产品设计、知识沉淀等工作。
> 本手册定义两块核心内容：① AI Native 产品工作流；② WDP 产品知识库索引。

---

## 第一部分：AI Native 产品工作流

### 定位

AI Native 工作流 = **AI 深度参与 WDP 产研工作的一套方式**（不是某个功能、不是设计逻辑、不是目标本身）。核心是改变产研工作方式：让 AI 参与从信号到长期跟踪的每个环节，实现三个转变：

1. 从"人追信息"→"信息主动分析并推送给人"
2. 末端设计自动化：功能性设计定稿后，末端产物由 AI 辅助生成，人做关键决策与把关
3. 流程统一、可共同维护、低繁琐，替代各自为战

### 四层闭环 + 项目分区（完整设计、分层落地，非 MVP 裁功能）

```
原始信息（会议纪要/客户反馈/聊天记录/语音）
    ↓  signal-intake skill 清洗（或对话框「📥 信号清洗模式」）
① 信号层     多源数据的收集与清洗 → 结构化信号
    ↓  requirement-triage skill 归并 + 人工快速校验
② 需求层     信号 → 候选需求 → 人工校验 → 建档 → 自动分配
    ↓  design-converge skill / 对话框「🎯 设计模式」
③ 产品设计层  面向 agent 的完整性设计 → 研发交付物
    ↓
④ 跟踪层     需求全生命周期节点跟踪 + 停滞提醒
    ↓  上线效果回流成新信号（形成闭环）

【项目分区】客户售前/售后项目（与四层闭环并行）
  信号池（统一入口）→ 沉淀为「项目需求(PREQ)」→ 交付材料(DLV, 售前/售中/售后)
  项目档案含：客户、商机号、阶段、负责人、BD、TB 对接人
```

**各层落点（写入 knowledge/ 目录）**：
| 层 | 产出目录 | 对应能力 |
|---|---|---|
| 信号层 | `knowledge/signals/` | signal-intake skill / 📥 信号清洗模式 |
| 需求层 | `knowledge/requirements/` | requirement-triage skill |
| 产品设计层 | `knowledge/designs/` | design-converge skill / 🎯 设计模式 |
| 跟踪层 | `knowledge/tracking/` | 定时任务扫描（需求停滞/周报） |
| 决策记录 | `knowledge/decisions/` | （贯穿全流程） |
| 项目 | `knowledge/projects/<项目>/` | 项目开档 + 信号→项目需求 + 交付材料 |
| 团队成员 | `knowledge/team/` | 能力画像随对话自动沉淀 |

### 关键认知

- **信号层**：多源数据格式不一、噪声多、重复多。关键在收集广度 + 清洗质量：转写、结构化、去重、归一。产出干净、结构化、可被后续直接使用的信号流。**保留事实、剔除情绪；拆分复合信号；标注可信度（一手客户 > 转述 > 推测）**。
- **需求层**：AI 把信号归并成候选需求，但**保留人工快速校验入口**——人能一眼确认"是不是真需求、归类对不对"，校验成本要低。校验通过即建档（带标签/来源/优先级，可追溯），并根据模块自动分配给对应负责人。
- **产品设计层**：核心认知——**产品设计产物的读者不只是人，主要面向 agent**。设计要做到"agent 能读懂、能据此执行"的完整性：结构化、无歧义，把 agent 执行所需信息（数据契约、状态、边界、执行规则）补齐。
- **跟踪层**：基于需求档案做自动化节点跟踪，各节点状态自动更新；到节点/超期/停滞时主动提醒；上线效果数据回流成新信号。

### 工作原则（团队铁律）

1. **单一数据源**：所有团队知识资产只落在 `knowledge/` 一个目录，git 版本化，不搞第二份清单/备份。
2. **人工介入点低门槛**：人只在关键处介入（信号确认、需求校验、设计把关），复杂度交给 AI 后台承担。
3. **面向 agent 的完整性**：设计稿必须包含 agent 执行所需的全部信息，通过 skill 模板强制。
4. **可追溯**：需求能追到源信号，信号能追到原始信息，设计能追到需求。
5. **复用平台已有能力**：承接现有底座（Agent 调度、模型配置、记忆机制、数据接入），不重复造。

### 可用 skills（团队 skill，`skills/` 目录）

- `signal-intake` — 信号清洗（多源原始信息 → 结构化信号）
- `requirement-triage` — 需求校验建档
- `design-converge` — 设计收敛工作流（🎯 设计模式激活；批量拷问→不可逆决策→零歧义方案文档）
- `wdp-online-knowledge-sync` — WDP 在线知识源同步
- `wdp-workbench-ui` — 工作台 UI 规范

使用时对 agent 说明任务（如"用 signal-intake 清洗这段会议纪要"），或用对话框的能力模式按钮激活；agent 会加载对应 skill 按规则执行。
团队 skill 由 admin 在「团队 Agent」页用「技能助手」对话编辑、发布（发布前有 frontmatter 校验 + diff 展示）。

### 智能体体系（详见 docs/智能体档案-01/02）

- **对话 Agent**：每位成员的私人产研助手。除执行上述工作流外，还能**查询知识库**（"知识库现状/某项目进展/谁负责什么"，走 query_knowledge.py）、**沉淀成员画像**（管理员直述成员能力时强制记录）、**沉淀个人技能**。
- **4 个专职 Agent（admin 用，团队 Agent 页）**：归并助手 / 审核助手（含派单建议）/ 团队规则助手 / 团队技能助手。
- **定时任务（6 个内置，纯脚本机械化，默认关，admin 按需开）**：信号清洗 / 需求停滞提醒 / 需求周报 / 上传处置 / 清理已删 / 刷新知识库索引。提醒类任务扫描后主动通知 admin/负责人。

---

## 第二部分：WDP 产品知识库索引

> WDP（数字孪生 PaaS 平台）是基于 UE 云渲染技术的数字孪生场景构建与交付平台。
> ⚠️ 本索引是**快照 + 在线源路由**。产品知识以在线源为准；在线源需配置对应 MCP（企微文档/飞书）后才能实时拉取，未配置时以本快照为背景认知，关键数据需人工核对。

### WDP 平台概览

WDP5 产品入口四大模块：
- **我的案例**：创建/编辑/预览/发布/删除案例，创建副本
- **交付给我的**：管理云端交付的案例，支持运行/分享链接
- **我的权益**：查看个人/团队订阅权益
- **我的团队管理**：团队空间创建/切换/成员/权限/席位管理

整体闭环完成度约 75%（2026-04 口径）。前场能力（场景生产、业务表达、镜头讲述）已成熟，后场链路（数据前处理、工具化入口）是主要短板。

### 编辑器五大主线能力

| 链路 | 核心能力 |
|------|---------|
| ① 进入编辑与项目准备 | 顶部栏工作流、渲染设置、模块切换 |
| ② 空间底座构建 | 官方底板（AES城市底板）、空间底板（3DTiles/WMS/WMTS/WFS） |
| ③ 空间内容生产 | DTA模型资产库、用户模型导入（14种格式）、BIM构件树、动画模型、场景大纲 |
| ④ 场景表达增强 | 智能建模（植被/岸堤/水体）、材质覆盖、覆盖物（POI/路径/区域/窗口/可视域/3D文字）、环境模板 |
| ⑤ 交互与联动编排 | 固定镜头、镜头漫游、API调试面板、镜头API代码复制 |

**五大关键对象**：底板、模型（最强主线）、覆盖物、镜头、API联调。

⚠️ **已废弃模块**（不对外、不作能力介绍、规划白皮书一律排除）：画布、蓝图、孪生体管理、GUI扩展（SuperGUI 系）、GUI/超级GUI。编辑器现为**五大主线**（非六大）。

### API 能力

- **200+ API 方法，14 大技术分类**，JavaScript SDK（npm 前端调用，非 RESTful/Java/Python）
- ⚠️ **版本门禁铁律**：编码以**发布版**为准（`wdpapidoc.51aes.com/apifunc/wdpapi`，v2.3.1），Skill 库（`wdpapi-skill.51aes.com`）是前瞻路由参考，含未发布 API。
- 14大分类：场景初始化/渲染工厂/环境工厂/相机/覆盖物(19种)/模型(14种)/场景管理/工具/数据模型/系统类/UI组件/BIM插件/GIS插件/WIM插件/实体动画。

### 报价体系（三个标准版本）

| 版本 | 年费 | 续费折扣 |
|------|------|---------|
| 标准版 | 18.8万/年 | 9折 |
| 尊享版 | 38.8万/年 | 8折 |
| 高级定制版 | 88.8万/年 | 定制 |

五种合作模式：平台标准合作 / 平台+工程定制(O+P)体验版 / O+P常规 / OEM定制 / 云管线+私有化。

### 部署模式

| 模式 | 时间 | 说明 |
|------|------|------|
| 公有云SaaS | 即时 | 51World云端 |
| Windows单机版 | — | 1路渲染+1路转换 |
| Windows集群版 | ~9人天 | 中心云+多GPU节点 |
| 国产化全量私有化 | ~1周 | 海光CPU+麒麟OS+金仓DB+RTX4080 |
| 国产化离线Lite | ≤1天 | 自动化部署，仅运行展示 |

⚠️ **国产化口径**：从 5.6 版本起支持，OS=银河麒麟V10 SP3，DB=**金仓 KingbaseES**（不是达梦）。云渲染是串流（非 WebGL）。

### 知识工坊（前瞻产品，在研）

⚠️ **产品正式中文名 = 「知识工坊」**；"Workshop" 是活动名，对外/汇报材料不要把 Workshop 当产品名突出。
- **S1/S2 是季度阶段（Season），不是产品版本号**——产品版本一直是内测 α V1。
- 最新最全说明看飞书《Workshop S2 完整说明报告》（doc_token=`WMmwdAWOIo2GyQxpHN7c3EsqnRg`）。旧《Workshop Alpha V1 PRD》已被 S2 报告取代。
- 五大核心模块：数据标注 / 空间图谱(Neo4j) / 业务Skill（支持用户自定义） / Agent联调(AgentLoop) / 二次开发。另含 Memory 机制 + 自定义 LLM/ASR/TTS。

### WDP 战略定位

- WDP = AES 矩阵中**唯一的「开放平台」层**（AES 底座/Clonova/51WM/AperOne 皆引擎或应用型）。
- **汇报铁律**：WDP 汇报必突出「面向生态的开放平台」。
- AI Native WDP = 新形态平台产品，早期策划勿明说"WDP6"。

### 在线源路由（需配置对应 MCP 后可实时拉取）

| 维度 | 在线源 | 工具 |
|------|--------|------|
| 编辑器手册 | 飞书 Wiki `zaqa9535mw4.feishu.cn/wiki/wikcnmC72WN01k0vWsr54RbCWXg` | 飞书 Open API |
| API 发布版 | `wdpapidoc.51aes.com/apifunc/wdpapi`（v2.3.1，权威） | 浏览器/HTTP |
| API Skill库 | `wdpapi-skill.51aes.com`（前瞻） | HTTP 直读 |
| 报价/流程规则 | 企微文档（WDP流程规则 + 报价表） | 企微文档 MCP |
| 版本/任务 | 企微（版本发版索引 + 研发任务日志） | 企微文档 MCP |

> 📌 团队实例若要实时拉在线源，需在 config 配置企微文档 MCP / 飞书 API 凭证。未配置时，产品知识以本快照为背景认知，精确数据（版本号/报价/API清单）需人工到在线源核对。

### 汇报口径铁律（涉及 WDP 汇报时必守）

1. **AI Native** = AI 深度参与产研工作的一套方式，别下定义、别上课，从目标出发引出模块，尽量不点"AI Native"这个词本身。
2. **成果/数据表述须诚实**：勿把行业案例中的相似概念雏形说成平台"已支持/已沉淀"，应表述为"已有雏形→下一步基于平台结构化梳理、沉淀为技术资产"。
3. **数据可按有利口径挑好说但须真实来源**，不虚标、不用会缩水的错口径。
4. WDP 汇报必突出「面向生态的开放平台」定位。

---

## 附：如何使用本手册

- **团队成员**：直接对话提出工作需求（"帮我整理这段客户反馈成信号"、"WDP 国产化支持哪些数据库"），agent 会依据本手册的工作流和知识库应答。
- **agent**：每轮对话已通过 memory 加载本手册要点。涉及具体 WDP 产品数据时，优先提示需在线核对；涉及产研工作流时，按四层闭环 + skills 执行。
