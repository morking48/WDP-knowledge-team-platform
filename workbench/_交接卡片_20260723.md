# WDP团队工作台开发 · 会话交接卡片（2026-07-23）

> **为什么开新会话**：原会话 `20260723_095059_458c54`（3248条/7.7MB）过大，导致编辑框秒刷复原、界面闪烁。数据没丢，退役换新会话继续。
> **新会话开场**：读本卡片 + 工程 workbench/ 下4份文档即可无缝接手。

## ⚠️ 铁律（避免上个会话的问题）
1. **只在 `E:\wdp-team-hermes` 工程里动**，绝不碰个人 hermes（E:\HermesAgent）。两个实例严格隔离。
2. 固定用 **copilot（claude-opus-4.8）**，别用会截断的渠道。
3. 别自己大量读图/vision回看，改完我自己在浏览器测。
4. 盘符已迁 F→E，所有路径用 E 盘。

## 工程与文档
- **工程根**：`E:\wdp-team-hermes`
- **必读文档**（workbench/ 下，都是最新）：
  - `TODO-alignment.md`（2026-07-22，开发对齐清单，P0-P3完成情况最全）
  - `HANDOVER.md`（项目目标+架构决策）
  - `TASK-LIST.md`、`F1-frontend-design.md`、`MIGRATION-GUIDE.md`
- **两个gitlab仓库**（已推送）：
  - platform：`gitlab.51cloud.local/Maguanjie/wdp-team-platform`
  - knowledge：`gitlab.51cloud.local/Maguanjie/wdp-team-knowledge`

## 项目一句话
把单机版 Hermes WebUI 升级为 **WDP 产品团队多用户 AI 工作台**：多用户认证+角色隔离、5大视图（对话/工作台/入库审核/成员管理/个人中心）、三大工作组件（信号/需求/设计）走知识库闭环、主/子agent分层、公网K8s部署。

## 开发进度
**主体功能全部完成**（P0生命周期流转/P1可追溯派活/P2统计+知识库互动/P3主agent定时任务，TODO-alignment.md 里 ✅ 一片）。
**当前阶段：多轮回归测试 + 修bug + 模拟数据重置**。

## 🔴 最新一轮测试发现的待修bug（会话末尾，接手先处理这些）
1. **对话中 @ 需要显示团队成员列表**（现在靠手打不可靠）
2. **个人中心配了工作库，但对话中工作库不响应、没切换**
3. **对话提交信号后，管理员的决策流程**（待理顺）
4. **界面排版**：表格上下不对齐、过于贴边等
5. **关联信息/分配角色**：要有既定对象列表可选（不靠手打）
6. **模拟数据重置**：session/成员/工作台池子的测试数据要重置到够测试的量
7. 之前提的：归并和审计合并（简化操作逻辑，已同意按建议做）

## 明确延后（本期不做）
自定义个人skills、跨agent产品讨论、拖拽改状态交互。

## 部署待办（交运维，非本地开发）
init-knowledge.sh对齐新结构、入库自动push gitlab、K8s Secret挂git凭证、主agent独立部署。

## 测试账号
新会话里让AI从工程配置读（users.json），或问我。

---
读完先跟我说：你理解的当前进度 + 下一步先修哪个bug，等我确认再动手。
